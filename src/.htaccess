RewriteEngine On

# If file or folder exists, serve it normally
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Otherwise, send everything to React
RewriteRule ^ index.html [QSA,L]